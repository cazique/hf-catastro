# -----------------------------------------------------------------------------
# VARIABLES DE ENTORNO - SISTEMA CATASTRAL HOGAR FAMILIAR
#
# Copia este fichero a .env y ajusta los valores según tu configuración.
# -----------------------------------------------------------------------------

# -- Configuración General --
# URL pública de la aplicación. Usada para generar enlaces absolutos.
# E.g., https://misitio.com/sistema-catastral
APP_URL="http://localhost"

# -- Configuración de la Caché (Opcional) --
# Activa o desactiva la caché en base de datos.
# Si está en 'false', la aplicación no intentará conectar a la BD.
CACHE_DB_ENABLED=false

# Tiempo de vida (TTL) de la caché en segundos.
# Por defecto: 2592000 segundos (30 días)
CACHE_TTL_SECONDS=2592000

# -- Configuración de la Base de Datos (solo si CACHE_DB_ENABLED=true) --
DB_HOST="127.0.0.1"
DB_PORT="3306"
DB_DATABASE="catastro_cache"
DB_USERNAME="root"
DB_PASSWORD=""
DB_CHARSET="utf8mb4"

# -- Configuración del Cliente HTTP --
# User-Agent para las peticiones al Catastro.
HTTP_USER_AGENT="SistemaCatastralHogarFamiliar/1.0"

# Timeout de conexión en segundos.
HTTP_CONNECT_TIMEOUT=10

# Timeout total de la petición en segundos.
HTTP_TIMEOUT=30

# Activa/desactiva la verificación del certificado SSL.
# ¡¡¡ ATENCIÓN: NO RECOMENDADO PARA PRODUCCIÓN !!!
# Cambiar a 'false' solo para entornos de desarrollo con problemas de certificados.
HTTP_VERIFY_SSL=true

# -- Rate Limiting (Protección por IP) --
# Activa o desactiva el límite de peticiones por IP.
RATE_LIMIT_ENABLED=true

# Número máximo de peticiones permitidas por IP dentro del periodo de tiempo.
RATE_LIMIT_MAX_REQUESTS=100

# Periodo de tiempo para el rate limiting en segundos.
# Por defecto: 3600 segundos (1 hora)
RATE_LIMIT_PERIOD_SECONDS=3600